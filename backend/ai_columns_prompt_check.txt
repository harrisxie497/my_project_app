====================================================================================================
检查所有AI列的提示词配置（详细版）
====================================================================================================

找到 6 个AI类型的列

====================================================================================================
第 1 个AI列
====================================================================================================
列名: H
文件类型: CUSTOMS
操作类型: NONE
规则引用: ['policy_ai_goods_en']
源列: ['H']

----------------------------------------------------------------------------------------------------
field_pipelines表中的rule_params_json:
----------------------------------------------------------------------------------------------------
{
  "policy_ai_goods_en": {
    "prompt": "你是一个专业的日英翻译专家。请将以下日文品名翻译成英文。\n\n要求：\n1. 翻译要准确、专业\n2. 不要包含特殊字符（/、\\等）\n3. 长度不超过60个字符\n4. 每行一个翻译结果，按顺序对应\n5. 只返回翻译结果，不要包含序号（如\"1.\"、\"2.\"等）\n6. 只返回翻译结果，不要包含其他文字\n7. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出\n8. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素\n9. 必须严格按顺序返回{input_count}个元素，不能多也不能少"
  }
}

----------------------------------------------------------------------------------------------------
rule_definitions表中的schema_json:
----------------------------------------------------------------------------------------------------
{
  "desc": "品名：去括号备注→英译→大写→去冗余（后台固定流程）",
  "handler": "ai.goods_name_en",
  "configurable_params": {
    "system_prompt": "输入的数组数据是“品名”，也就是购买的商品名称，这是一个数组数组，针对每一个元素，删除括号内内容（如“AIRPLANE TOY (L码)”→“AIRPLANE TOY”），英文名称统一大写（“cotton t-shirt”→“COTTON T-SHIRT”），前如果前后二个是重复含义，保留一个即可； 对于输出的要求，也是一个数组，并且顺序和数组长度保持输入的一样。 "
  }
}

----------------------------------------------------------------------------------------------------
重复检查:
----------------------------------------------------------------------------------------------------
  [警告] rule_params_json.policy_ai_goods_en 中包含 prompt 配置，应该删除
  prompt 内容:
你是一个专业的日英翻译专家。请将以下日文品名翻译成英文。

要求：
1. 翻译要准确、专业
2. 不要包含特殊字符（/、\等）
3. 长度不超过60个字符
4. 每行一个翻译结果，按顺序对应
5. 只返回翻译结果，不要包含序号（如"1."、"2."等）
6. 只返回翻译结果，不要包含其他文字
7. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出
8. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素
9. 必须严格按顺序返回{input_count}个元素，不能多也不能少

  [信息] 规则定义中包含 system_prompt
  system_prompt 内容:
输入的数组数据是“品名”，也就是购买的商品名称，这是一个数组数组，针对每一个元素，删除括号内内容（如“AIRPLANE TOY (L码)”→“AIRPLANE TOY”），英文名称统一大写（“cotton t-shirt”→“COTTON T-SHIRT”），前如果前后二个是重复含义，保留一个即可； 对于输出的要求，也是一个数组，并且顺序和数组长度保持输入的一样。 


====================================================================================================
第 2 个AI列
====================================================================================================
列名: I
文件类型: CUSTOMS
操作类型: NONE
规则引用: ['policy_ai_material_en']
源列: ['I']

----------------------------------------------------------------------------------------------------
field_pipelines表中的rule_params_json:
----------------------------------------------------------------------------------------------------
{
  "policy_ai_material_en": {
    "prompt": "你是一个专业的日英翻译专家。请将以下日文材质翻译成英文。\n\n要求：\n1. 翻译要准确、专业\n2. 转换为标准材质代码（如：COTTON、POLYESTER等）\n3. 每行一个翻译结果，按顺序对应\n4. 只返回翻译结果，不要包含序号（如\"1.\"、\"2.\"等）\n5. 只返回翻译结果，不要包含其他文字\n6. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出\n7. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素\n8. 必须严格按顺序返回{input_count}个元素，不能多也不能少"
  }
}

----------------------------------------------------------------------------------------------------
rule_definitions表中的schema_json:
----------------------------------------------------------------------------------------------------
{
  "desc": "材质：去括号备注→英译大写→材质替换表置换（后台固定流程）",
  "handler": "ai.material_translate_and_substitute",
  "configurable_params": {
    "system_prompt": "输入的数组数据是“材质”，也就是购买的商品名称的原材料，这是一个数组数组，针对每一个元素，删除括号内注释（如“POLYESTER (含棉30%)”→“POLYESTER”），中文材质名按行业标准英文大写（“聚酯纤维”→“POLYESTER FIBER”），前如果前后二个是重复含义，保留一个即可，同时你也是日本海关材料专家，对于同类型的材质，可以进行替换选择税汇率较低的材质； 对于输出的要求，也是一个数组，并且顺序和数组长度保持输入的一样。"
  }
}

----------------------------------------------------------------------------------------------------
重复检查:
----------------------------------------------------------------------------------------------------
  [警告] rule_params_json.policy_ai_material_en 中包含 prompt 配置，应该删除
  prompt 内容:
你是一个专业的日英翻译专家。请将以下日文材质翻译成英文。

要求：
1. 翻译要准确、专业
2. 转换为标准材质代码（如：COTTON、POLYESTER等）
3. 每行一个翻译结果，按顺序对应
4. 只返回翻译结果，不要包含序号（如"1."、"2."等）
5. 只返回翻译结果，不要包含其他文字
6. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出
7. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素
8. 必须严格按顺序返回{input_count}个元素，不能多也不能少

  [信息] 规则定义中包含 system_prompt
  system_prompt 内容:
输入的数组数据是“材质”，也就是购买的商品名称的原材料，这是一个数组数组，针对每一个元素，删除括号内注释（如“POLYESTER (含棉30%)”→“POLYESTER”），中文材质名按行业标准英文大写（“聚酯纤维”→“POLYESTER FIBER”），前如果前后二个是重复含义，保留一个即可，同时你也是日本海关材料专家，对于同类型的材质，可以进行替换选择税汇率较低的材质； 对于输出的要求，也是一个数组，并且顺序和数组长度保持输入的一样。


====================================================================================================
第 3 个AI列
====================================================================================================
列名: J
文件类型: CUSTOMS
操作类型: NONE
规则引用: ['policy_translate_from_targetcol_en_upper']
源列: ['K']

----------------------------------------------------------------------------------------------------
field_pipelines表中的rule_params_json:
----------------------------------------------------------------------------------------------------
{
  "policy_translate_from_targetcol_en_upper": {
    "prompt": "你是一个专业的日英翻译专家。请将以下日文翻译成英文。\n\n要求：\n1. 翻译要准确、专业\n2. 每行一个翻译结果，按顺序对应\n3. 只返回翻译结果，不要包含序号（如\"1.\"、\"2.\"等）\n4. 只返回翻译结果，不要包含其他文字\n5. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出\n6. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素\n7. 必须严格按顺序返回{input_count}个元素，不能多也不能少"
  }
}

----------------------------------------------------------------------------------------------------
rule_definitions表中的schema_json:
----------------------------------------------------------------------------------------------------
{
  "desc": "从目标列（X/Y）翻译为英文并大写（后台固定流程）",
  "handler": "ai.translate_from_targetcol_to_en_upper",
  "configurable_params": {
    "system_prompt": "你是一个专业的日英翻译专家。请将以下日文地址翻译成英文，并保持日本地址的格式。\n\n输入数据：\n{输入数据}\n\n要求：\n1. 翻译要准确、专业\n2. 地址层级完整：都道府县 → 市/区 → 町/地区 → 丁目/番地\n3. 例如：\"愛知県名古屋市中区1-2-3\" 应翻译为 \"Aichi Ken Nagoya Shi Naka Ku 1-2-3\"\n4. 例如：\"東京都渋谷区渋谷1-2-3\" 应翻译为 \"Tokyo To Shibuya Ku Shibuya 1-2-3\"\n5. 例如：\"大阪府大阪市中央区1-2-3\" 应翻译为 \"Osaka Fu Osaka Shi Chuo Ku 1-2-3\"\n6. 中间不需要加标点符号，只加入空格分隔各层级\n7. 门牌部分（如1-2-3）保持原格式，-两边都不需要有空格\n8. 翻译结果全部大写（全大写）\n9. 只返回翻译后的地址，每行一个，按顺序对应，不要包含JSON格式、序号或其他文字\n10. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出\n11. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素\n12. 必须严格按顺序返回{input_count}个元素，不能多也不能少"
  }
}

----------------------------------------------------------------------------------------------------
重复检查:
----------------------------------------------------------------------------------------------------
  [警告] rule_params_json.policy_translate_from_targetcol_en_upper 中包含 prompt 配置，应该删除
  prompt 内容:
你是一个专业的日英翻译专家。请将以下日文翻译成英文。

要求：
1. 翻译要准确、专业
2. 每行一个翻译结果，按顺序对应
3. 只返回翻译结果，不要包含序号（如"1."、"2."等）
4. 只返回翻译结果，不要包含其他文字
5. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出
6. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素
7. 必须严格按顺序返回{input_count}个元素，不能多也不能少

  [信息] 规则定义中包含 system_prompt
  system_prompt 内容:
你是一个专业的日英翻译专家。请将以下日文地址翻译成英文，并保持日本地址的格式。

输入数据：
{输入数据}

要求：
1. 翻译要准确、专业
2. 地址层级完整：都道府县 → 市/区 → 町/地区 → 丁目/番地
3. 例如："愛知県名古屋市中区1-2-3" 应翻译为 "Aichi Ken Nagoya Shi Naka Ku 1-2-3"
4. 例如："東京都渋谷区渋谷1-2-3" 应翻译为 "Tokyo To Shibuya Ku Shibuya 1-2-3"
5. 例如："大阪府大阪市中央区1-2-3" 应翻译为 "Osaka Fu Osaka Shi Chuo Ku 1-2-3"
6. 中间不需要加标点符号，只加入空格分隔各层级
7. 门牌部分（如1-2-3）保持原格式，-两边都不需要有空格
8. 翻译结果全部大写（全大写）
9. 只返回翻译后的地址，每行一个，按顺序对应，不要包含JSON格式、序号或其他文字
10. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出
11. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素
12. 必须严格按顺序返回{input_count}个元素，不能多也不能少


====================================================================================================
第 4 个AI列
====================================================================================================
列名: K
文件类型: CUSTOMS
操作类型: NONE
规则引用: ['policy_translate_from_targetcol_en_upper']
源列: ['N']

----------------------------------------------------------------------------------------------------
field_pipelines表中的rule_params_json:
----------------------------------------------------------------------------------------------------
{
  "policy_translate_from_targetcol_en_upper": {
    "prompt": "你是一个专业的日英翻译专家。请将以下日文地址翻译成英文，并保持日本地址的格式。\n\n输入数据：\n{输入数据}\n\n要求：\n1. 翻译要准确、专业\n2. 地址层级完整：都道府县 → 市/区 → 町/地区 → 丁目/番地\n3. 例如：\"愛知県名古屋市中区1-2-3\" 应翻译为 \"Aichi Ken Nagoya Shi Naka Ku 1-2-3\"\n4. 例如：\"東京都渋谷区渋谷1-2-3\" 应翻译为 \"Tokyo To Shibuya Ku Shibuya 1-2-3\"\n5. 例如：\"大阪府大阪市中央区1-2-3\" 应翻译为 \"Osaka Fu Osaka Shi Chuo Ku 1-2-3\"\n6. 中间不需要加标点符号，只加入空格分隔各层级\n7. 门牌部分（如1-2-3）保持原格式，-两边都不需要有空格\n8. 翻译结果全部大写（全大写）\n9. 只返回翻译后的地址，每行一个，按顺序对应，不要包含JSON格式、序号或其他文字\n10. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出\n11. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素\n12. 必须严格按顺序返回{input_count}个元素，不能多也不能少\n13. 输入是数组数据，输出必须也是数组，顺序和长度与输入相同\n14. 门牌号是最后一部分，后面不应该有任何信息（如\"宅配ボックス希望\"等）"
  }
}

----------------------------------------------------------------------------------------------------
rule_definitions表中的schema_json:
----------------------------------------------------------------------------------------------------
{
  "desc": "从目标列（X/Y）翻译为英文并大写（后台固定流程）",
  "handler": "ai.translate_from_targetcol_to_en_upper",
  "configurable_params": {
    "system_prompt": "你是一个专业的日英翻译专家。请将以下日文地址翻译成英文，并保持日本地址的格式。\n\n输入数据：\n{输入数据}\n\n要求：\n1. 翻译要准确、专业\n2. 地址层级完整：都道府县 → 市/区 → 町/地区 → 丁目/番地\n3. 例如：\"愛知県名古屋市中区1-2-3\" 应翻译为 \"Aichi Ken Nagoya Shi Naka Ku 1-2-3\"\n4. 例如：\"東京都渋谷区渋谷1-2-3\" 应翻译为 \"Tokyo To Shibuya Ku Shibuya 1-2-3\"\n5. 例如：\"大阪府大阪市中央区1-2-3\" 应翻译为 \"Osaka Fu Osaka Shi Chuo Ku 1-2-3\"\n6. 中间不需要加标点符号，只加入空格分隔各层级\n7. 门牌部分（如1-2-3）保持原格式，-两边都不需要有空格\n8. 翻译结果全部大写（全大写）\n9. 只返回翻译后的地址，每行一个，按顺序对应，不要包含JSON格式、序号或其他文字\n10. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出\n11. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素\n12. 必须严格按顺序返回{input_count}个元素，不能多也不能少"
  }
}

----------------------------------------------------------------------------------------------------
重复检查:
----------------------------------------------------------------------------------------------------
  [警告] rule_params_json.policy_translate_from_targetcol_en_upper 中包含 prompt 配置，应该删除
  prompt 内容:
你是一个专业的日英翻译专家。请将以下日文地址翻译成英文，并保持日本地址的格式。

输入数据：
{输入数据}

要求：
1. 翻译要准确、专业
2. 地址层级完整：都道府县 → 市/区 → 町/地区 → 丁目/番地
3. 例如："愛知県名古屋市中区1-2-3" 应翻译为 "Aichi Ken Nagoya Shi Naka Ku 1-2-3"
4. 例如："東京都渋谷区渋谷1-2-3" 应翻译为 "Tokyo To Shibuya Ku Shibuya 1-2-3"
5. 例如："大阪府大阪市中央区1-2-3" 应翻译为 "Osaka Fu Osaka Shi Chuo Ku 1-2-3"
6. 中间不需要加标点符号，只加入空格分隔各层级
7. 门牌部分（如1-2-3）保持原格式，-两边都不需要有空格
8. 翻译结果全部大写（全大写）
9. 只返回翻译后的地址，每行一个，按顺序对应，不要包含JSON格式、序号或其他文字
10. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出
11. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素
12. 必须严格按顺序返回{input_count}个元素，不能多也不能少
13. 输入是数组数据，输出必须也是数组，顺序和长度与输入相同
14. 门牌号是最后一部分，后面不应该有任何信息（如"宅配ボックス希望"等）

  [信息] 规则定义中包含 system_prompt
  system_prompt 内容:
你是一个专业的日英翻译专家。请将以下日文地址翻译成英文，并保持日本地址的格式。

输入数据：
{输入数据}

要求：
1. 翻译要准确、专业
2. 地址层级完整：都道府县 → 市/区 → 町/地区 → 丁目/番地
3. 例如："愛知県名古屋市中区1-2-3" 应翻译为 "Aichi Ken Nagoya Shi Naka Ku 1-2-3"
4. 例如："東京都渋谷区渋谷1-2-3" 应翻译为 "Tokyo To Shibuya Ku Shibuya 1-2-3"
5. 例如："大阪府大阪市中央区1-2-3" 应翻译为 "Osaka Fu Osaka Shi Chuo Ku 1-2-3"
6. 中间不需要加标点符号，只加入空格分隔各层级
7. 门牌部分（如1-2-3）保持原格式，-两边都不需要有空格
8. 翻译结果全部大写（全大写）
9. 只返回翻译后的地址，每行一个，按顺序对应，不要包含JSON格式、序号或其他文字
10. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出
11. 如果某个元素无法翻译，请返回空字符串，不要跳过该元素
12. 必须严格按顺序返回{input_count}个元素，不能多也不能少


====================================================================================================
第 5 个AI列
====================================================================================================
列名: X
文件类型: CUSTOMS
操作类型: NONE
规则引用: ['policy_ai_text_ja_clean']
源列: ['AD']

----------------------------------------------------------------------------------------------------
field_pipelines表中的rule_params_json:
----------------------------------------------------------------------------------------------------
{
  "policy_ai_text_ja_clean": {
    "prompt": "你是一个日文数据处理专家。请清理以下日文收件人名。\n\n要求：\n1. 移除敬语和称谓（様、様、先生、様方等）\n2. 标准化假名（平假名/片假名）\n3. 长度不超过40个字符\n4. 每行一个清理结果，按顺序对应\n5. 只返回清理后的名字，不要包含序号（如\"1.\"、\"2.\"等）\n6. 只返回清理后的名字，不要包含其他文字\n7. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出\n8. 如果某个元素无法清理，请返回空字符串，不要跳过该元素\n9. 必须严格按顺序返回{input_count}个元素，不能多也不能少"
  }
}

----------------------------------------------------------------------------------------------------
rule_definitions表中的schema_json:
----------------------------------------------------------------------------------------------------
{
  "desc": "收件人名（日文）清洗：去括号备注，输出更像常见日本名字（后台固定流程）",
  "handler": "ai.ja_name_clean",
  "configurable_params": {
    "system_prompt": "输入的数组数据是“收件人名（日文）”，这是一个数组数组，针对每一个元素，去除括号内非姓名部分（如“田中（太郎）”→“田中”；“山田花子（YAMADA HANAKO）”→“山田花子”）。若输入为非标准日本人名（如中文姓名、乱码），调整为常见格式（如“佐藤 健一”“铃木 花子”）；保持姓前名后结构，无法识别则虚构一个人日本名字替换原来的信息，如果明显不是日本人名，例如写的是公司名，或者地址名，则随机生成一个正常日本人名字； 对于输出的要求，也是一个数组，并且顺序和数组长度保持输入的一样。"
  }
}

----------------------------------------------------------------------------------------------------
重复检查:
----------------------------------------------------------------------------------------------------
  [警告] rule_params_json.policy_ai_text_ja_clean 中包含 prompt 配置，应该删除
  prompt 内容:
你是一个日文数据处理专家。请清理以下日文收件人名。

要求：
1. 移除敬语和称谓（様、様、先生、様方等）
2. 标准化假名（平假名/片假名）
3. 长度不超过40个字符
4. 每行一个清理结果，按顺序对应
5. 只返回清理后的名字，不要包含序号（如"1."、"2."等）
6. 只返回清理后的名字，不要包含其他文字
7. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出
8. 如果某个元素无法清理，请返回空字符串，不要跳过该元素
9. 必须严格按顺序返回{input_count}个元素，不能多也不能少

  [信息] 规则定义中包含 system_prompt
  system_prompt 内容:
输入的数组数据是“收件人名（日文）”，这是一个数组数组，针对每一个元素，去除括号内非姓名部分（如“田中（太郎）”→“田中”；“山田花子（YAMADA HANAKO）”→“山田花子”）。若输入为非标准日本人名（如中文姓名、乱码），调整为常见格式（如“佐藤 健一”“铃木 花子”）；保持姓前名后结构，无法识别则虚构一个人日本名字替换原来的信息，如果明显不是日本人名，例如写的是公司名，或者地址名，则随机生成一个正常日本人名字； 对于输出的要求，也是一个数组，并且顺序和数组长度保持输入的一样。


====================================================================================================
第 6 个AI列
====================================================================================================
列名: Y
文件类型: CUSTOMS
操作类型: NONE
规则引用: ['policy_ai_text_dress_clean']
源列: ['M']

----------------------------------------------------------------------------------------------------
field_pipelines表中的rule_params_json:
----------------------------------------------------------------------------------------------------
{
  "policy_ai_text_dress_clean": {
    "prompt": "请将以下日文地址格式化，保持日文格式，不要翻译成英文。\n\n要求：\n1. 地址层级完整：都道府县 → 市/区 → 町/地区 → 丁目/番地\n2. 例如：\"愛知県名古屋市中区1-2-3\" 应格式化为 \"愛知県名古屋市中区1-2-3\"\n3. 例如：\"東京都渋谷区渋谷1-2-3\" 应格式化为 \"東京都渋谷区渋谷1-2-3\"\n4. 例如：\"大阪府大阪市中央区1-2-3\" 应格式化为 \"大阪府大阪市中央区1-2-3\"\n5. 中间不需要加标点符号，只加入空格分隔各层级\n6. 门牌部分（如1-2-3）保持原格式，-两边都不需要有空格\n7. 保持日文格式，不要翻译成英文（罗马字）\n8. 只返回格式化后的地址，每行一个，按顺序对应，不要包含JSON格式、序号或其他文字\n9. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出\n10. 如果某个元素无法格式化，请返回空字符串，不要跳过该元素\n11. 必须严格按顺序返回{input_count}个元素，不能多也不能少"
  }
}

----------------------------------------------------------------------------------------------------
rule_definitions表中的schema_json:
----------------------------------------------------------------------------------------------------
{
  "desc": "收件人地址（日文）翻译：翻译为英文（罗马大写），保持标准地址格式（后台固定流程）",
  "handler": "ai.ja_address_clean",
  "configurable_params": {
    "system_prompt": "你是一个日文地址整理专家，请将以下日文地址进行格式化整理并输出。\n有以下要求：\n1.地址的最后面需校验门牌格式，日本地址门牌格式（如\"4-10-25\"\"1-102B\"），无法解析则虚拟合理门牌格式（如\"1-10-25\"），如果门牌号码为空，请虚构后面的门牌号码，门牌号码之间用-链接。\n2.最后按照日本标准地址格式输出（如\"东京都渋谷区道玄坂1-10-25\"），在门牌的部分不能有空格，门牌号码后面不在有其他的信息（例如：1-10-101这种就很好， 在-两边都不需要有空格）\n3.中间不需要加标点符号，原有地址输入中有空格也不需要管。\n4.输入有{input_count}个元素，输出必须保持{input_count}个元素，必须严格按顺序返回{input_count}个元素，不能多也不能少。"
  }
}

----------------------------------------------------------------------------------------------------
重复检查:
----------------------------------------------------------------------------------------------------
  [警告] rule_params_json.policy_ai_text_dress_clean 中包含 prompt 配置，应该删除
  prompt 内容:
请将以下日文地址格式化，保持日文格式，不要翻译成英文。

要求：
1. 地址层级完整：都道府县 → 市/区 → 町/地区 → 丁目/番地
2. 例如："愛知県名古屋市中区1-2-3" 应格式化为 "愛知県名古屋市中区1-2-3"
3. 例如："東京都渋谷区渋谷1-2-3" 应格式化为 "東京都渋谷区渋谷1-2-3"
4. 例如："大阪府大阪市中央区1-2-3" 应格式化为 "大阪府大阪市中央区1-2-3"
5. 中间不需要加标点符号，只加入空格分隔各层级
6. 门牌部分（如1-2-3）保持原格式，-两边都不需要有空格
7. 保持日文格式，不要翻译成英文（罗马字）
8. 只返回格式化后的地址，每行一个，按顺序对应，不要包含JSON格式、序号或其他文字
9. 输入有{input_count}个元素，输出必须保持{input_count}个元素，不要偷懒输出
10. 如果某个元素无法格式化，请返回空字符串，不要跳过该元素
11. 必须严格按顺序返回{input_count}个元素，不能多也不能少

  [信息] 规则定义中包含 system_prompt
  system_prompt 内容:
你是一个日文地址整理专家，请将以下日文地址进行格式化整理并输出。
有以下要求：
1.地址的最后面需校验门牌格式，日本地址门牌格式（如"4-10-25""1-102B"），无法解析则虚拟合理门牌格式（如"1-10-25"），如果门牌号码为空，请虚构后面的门牌号码，门牌号码之间用-链接。
2.最后按照日本标准地址格式输出（如"东京都渋谷区道玄坂1-10-25"），在门牌的部分不能有空格，门牌号码后面不在有其他的信息（例如：1-10-101这种就很好， 在-两边都不需要有空格）
3.中间不需要加标点符号，原有地址输入中有空格也不需要管。
4.输入有{input_count}个元素，输出必须保持{input_count}个元素，必须严格按顺序返回{input_count}个元素，不能多也不能少。


====================================================================================================
检查完成
====================================================================================================
