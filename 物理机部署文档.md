# 日本清关Excel自动生成系统 - 物理机部署文档

## 目录
1. [系统要求](#1-系统要求)
2. [拉取最新代码](#2-拉取最新代码)
3. [服务器环境准备](#3-服务器环境准备)
4. [后端部署](#4-后端部署)
5. [前端部署](#6-前端部署)
6. [Nginx配置](#6-nginx配置)
7. [使用Systemd管理服务](#7-使用systemd管理服务)
8. [配置防火墙](#8-配置防火墙)
9. [SSL证书配置](#9-ssl证书配置)
10. [验证部署](#10-验证部署)
11. [常用维护命令](#11-常用维护命令)
12. [备份和恢复](#12-备份和恢复)
13. [监控和日志](#13-监控和日志)
14. [故障排查](#14-故障排查)
15. [性能优化](#15-性能优化)
16. [安全建议](#16-安全建议)

## 1. 系统要求

### 1.1 硬件要求
- CPU：4核或更高
- 内存：8GB或更高
- 磁盘空间：50GB或更高
- 网络：稳定的互联网连接

### 1.2 软件要求
- 操作系统：Ubuntu 20.04 LTS 或更高版本
- Python：3.10 或更高版本
- Node.js：18 或更高版本
- MySQL：8.0 或更高版本
- Nginx：1.18 或更高版本
- Git：2.0 或更高版本

## 2. 拉取最新代码

```bash
# 进入项目目录
cd /opt/japan-customs-system/my_project_app

# 拉取最新代码
git pull origin master
```

## 3. 服务器环境准备

### 3.1 更新系统
```bash
# 更新软件包列表
sudo apt update

# 升级已安装的软件包
sudo apt upgrade -y

# 清理不需要的软件包
sudo apt autoremove -y
```

### 3.2 安装基础软件
```bash
# 安装 Python 3.10
sudo apt install software-properties-common -y
sudo add-apt-repository ppa:deadsnakes/ppa -y
sudo apt update
sudo apt install python3.10 python3.10-venv python3.10-dev python3-pip -y

# 验证 Python 版本
python3.10 --version

# 安装 Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install nodejs -y

# 验证 Node.js 版本
node --version
npm --version

# 安装 Git
sudo apt install git -y

# 验证 Git 版本
git --version

# 安装 MySQL
sudo apt install mysql-server -y

# 验证 MySQL 版本
mysql --version

# 安装 Nginx
sudo apt install nginx -y

# 验证 Nginx 版本
nginx -v

# 安装其他依赖
sudo apt install build-essential libmysqlclient-dev pkg-config -y
```

### 3.3 创建项目目录
```bash
# 创建项目目录
sudo mkdir -p /opt/japan-customs-system

# 设置目录权限
sudo chown $USER:$USER /opt/japan-customs-system

# 进入项目目录
cd /opt/japan-customs-system
```

### 3.4 配置 MySQL
```bash
# 启动 MySQL 服务
sudo systemctl start mysql
sudo systemctl enable mysql

# 运行安全配置脚本
sudo mysql_secure_installation

# 按照提示完成配置：
# - 设置 root 密码
# - 移除匿名用户
# - 禁止 root 远程登录
# - 移除测试数据库
# - 重新加载权限表
```

## 4. 后端部署

### 4.1 克隆代码仓库
```bash
# 克隆代码
cd /opt/japan-customs-system
git clone https://github.com/harrisxie497/my_project_app.git

# 进入后端目录
cd my_project_app/backend
```

### 4.2 创建 Python 虚拟环境
```bash
# 创建虚拟环境
python3.10 -m venv .venv

# 激活虚拟环境
source .venv/bin/activate

# 验证虚拟环境
which python
which pip
```

### 4.3 安装依赖
```bash
# 升级 pip
pip install --upgrade pip

# 安装项目依赖
pip install -r requirements.txt

# 验证安装
pip list
```

### 4.4 配置环境变量
```bash
# 复制环境变量模板
cp .env.example .env

# 编辑 .env 文件
nano .env
```

**.env 配置示例：**
```bash
# 数据库配置
DATABASE_URL=mysql+pymysql://app:app123456@localhost:3306/demo?charset=utf8mb4

# JWT配置
SECRET_KEY=your-secret-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# DeepSeek AI配置
DEEPSEEK_API_KEY=your-deepseek-api-key-here
DEEPSEEK_API_URL=https://api.deepseek.com/v1

# 文件存储配置
STORAGE_PATH=/opt/japan-customs-system/storage
TASKS_STORAGE_PATH=/opt/japan-customs-system/storage/tasks

# 日志配置
LOG_LEVEL=INFO
LOG_DIR=/opt/japan-customs-system/my_project_app/backend/logs
```

### 4.5 创建存储目录
```bash
# 创建存储目录
mkdir -p /opt/japan-customs-system/storage/tasks

# 设置目录权限
chmod 755 /opt/japan-customs-system/storage
chmod 755 /opt/japan-customs-system/storage/tasks

# 创建日志目录
mkdir -p /opt/japan-customs-system/my_project_app/backend/logs
```

### 4.6 初始化数据库
```bash
# 创建数据库
sudo mysql -u root -p -e "CREATE DATABASE demo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 创建数据库用户
sudo mysql -u root -p -e "CREATE USER 'app'@'localhost' IDENTIFIED BY 'app123456';"

# 授予权限
sudo mysql -u root -p -e "GRANT ALL PRIVILEGES ON demo.* TO 'app'@'localhost';"

# 刷新权限
sudo mysql -u root -p -e "FLUSH PRIVILEGES;"

# 验证数据库连接
mysql -u app -papp123456 -e "SHOW DATABASES;"

# 初始化数据库表
python -c "from app.core.database import init_db; init_db()"
```

### 4.7 测试后端服务
```bash
# 手动启动测试
uvicorn app.main:app --host 0.0.0.0 --port 8000

# 在另一个终端测试
curl http://localhost:8000/api/v1/health

# 测试 API 文档
curl http://localhost:8000/docs

# 按 Ctrl+C 停止服务
```

## 5. 前端部署

### 5.1 进入前端目录
```bash
cd /opt/japan-customs-system/my_project_app/frontend
```

### 5.2 安装依赖
```bash
# 安装 npm 依赖
npm install

# 验证安装
npm list --depth=0
```

### 5.3 修改 API 地址
```bash
# 编辑 vite.config.js
nano vite.config.js

# 修改代理配置
```

**vite.config.js 配置示例：**
```javascript
export default defineConfig({
  plugins: [vue()],
  server: {
    host: '0.0.0.0',
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  }
})
```

### 5.4 构建前端
```bash
# 构建生产版本
npm run build

# 验证构建结果
ls -la dist/
```

### 5.5 部署静态文件
```bash
# 备份原有文件（如果存在）
sudo mv /var/www/html /var/www/html.backup 2>/dev/null || true

# 创建新的 web 目录
sudo mkdir -p /var/www/html

# 复制构建文件到 Nginx 目录
sudo cp -r dist/* /var/www/html/

# 设置文件权限
sudo chown -R www-data:www-data /var/www/html
sudo chmod -R 755 /var/www/html

# 验证文件
ls -la /var/www/html/
```

## 6. Nginx配置

### 6.1 创建 Nginx 配置文件
```bash
# 创建配置文件
sudo nano /etc/nginx/sites-available/japan-customs-system
```

**配置内容：**
```nginx
# HTTP 重定向到 HTTPS（可选）
server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名或IP

    # 如果没有配置 SSL，注释掉下面的重定向
    # return 301 https://$server_name$request_uri;

    # 前端静态文件
    location / {
        root /var/www/html;
        index index.html;
        try_files $uri $uri/ /index.html;

        # 启用 gzip 压缩
        gzip on;
        gzip_vary on;
        gzip_min_length 1024;
        gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;
    }

    # 后端 API 代理
    location /api {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # 文件下载代理
    location /storage {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 大文件上传设置
        client_max_body_size 100M;
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
    }

    # 日志配置
    access_log /var/log/nginx/japan-customs-access.log;
    error_log /var/log/nginx/japan-customs-error.log;
}

# HTTPS 配置（可选，需要 SSL 证书）
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     # 其他配置同上...
# }
```

### 6.2 启用配置
```bash
# 创建符号链接
sudo ln -s /etc/nginx/sites-available/japan-customs-system /etc/nginx/sites-enabled/

# 删除默认配置（可选）
sudo rm /etc/nginx/sites-enabled/default

# 测试配置文件
sudo nginx -t

# 如果测试通过，重载 Nginx
sudo systemctl reload nginx

# 重启 Nginx
sudo systemctl restart nginx
```

### 6.3 配置 Nginx 主配置
```bash
# 编辑主配置文件
sudo nano /etc/nginx/nginx.conf

# 添加或修改以下配置
```

**nginx.conf 关键配置：**
```nginx
user www-data;
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

http {
    # 基础配置
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Gzip 压缩
    gzip on;
    gzip_disable "msie6";
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss;

    # 包含站点配置
    include /etc/nginx/sites-enabled/*;
}
```

## 7. 使用Systemd管理服务

### 7.1 创建后端服务文件
```bash
# 创建服务文件
sudo nano /etc/systemd/system/japan-backend.service
```

**服务配置：**
```ini
[Unit]
Description=Japan Customs System Backend
After=network.target mysql.service

[Service]
Type=simple
User=your-username
Group=your-username
WorkingDirectory=/opt/japan-customs-system/my_project_app/backend
Environment="PATH=/opt/japan-customs-system/my_project_app/backend/.venv/bin"
EnvironmentFile=/opt/japan-customs-system/my_project_app/backend/.env
ExecStart=/opt/japan-customs-system/my_project_app/backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal
SyslogIdentifier=japan-backend

# 安全设置
NoNewPrivileges=true
PrivateTmp=true

[Install]
WantedBy=multi-user.target
```

### 7.2 启动服务
```bash
# 重新加载 systemd 配置
sudo systemctl daemon-reload

# 启用开机自启
sudo systemctl enable japan-backend

# 启动服务
sudo systemctl start japan-backend

# 查看服务状态
sudo systemctl status japan-backend

# 查看服务日志
sudo journalctl -u japan-backend -f
```

### 7.3 创建定时任务（可选）
```bash
# 编辑 crontab
crontab -e

# 添加定时任务
# 每天凌晨2点备份数据库
0 2 * * * /opt/japan-customs-system/scripts/backup_database.sh

# 每周日凌晨3点清理日志
0 3 * * 0 /opt/japan-customs-system/scripts/cleanup_logs.sh
```

## 8. 配置防火墙

### 8.1 使用 UFW 防火墙
```bash
# 启用 UFW
sudo ufw enable

# 允许 SSH
sudo ufw allow 22/tcp

# 允许 HTTP
sudo ufw allow 80/tcp

# 允许 HTTPS
sudo ufw allow 443/tcp

# 如果需要直接访问后端 API
sudo ufw allow 8000/tcp

# 查看防火墙状态
sudo ufw status

# 查看详细规则
sudo ufw status verbose
```

### 8.2 使用 iptables（可选）
```bash
# 查看 iptables 规则
sudo iptables -L -n -v

# 添加规则
sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 8000 -j ACCEPT

# 保存规则
sudo iptables-save | sudo tee /etc/iptables/rules.v4
```

## 9. SSL证书配置

### 9.1 使用 Let's Encrypt 免费证书
```bash
# 安装 Certbot
sudo apt install certbot python3-certbot-nginx -y

# 获取证书（自动配置 Nginx）
sudo certbot --nginx -d your-domain.com

# 按照提示完成配置：
# - 输入邮箱地址
# - 同意服务条款
# - 选择是否重定向 HTTP 到 HTTPS

# 测试证书续期
sudo certbot renew --dry-run
```

### 9.2 手动配置 SSL
```bash
# 生成自签名证书（仅用于测试）
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/ssl/private/japan-customs.key \
  -out /etc/ssl/certs/japan-customs.crt

# 设置权限
sudo chmod 600 /etc/ssl/private/japan-customs.key
sudo chmod 644 /etc/ssl/certs/japan-customs.crt
```

### 9.3 配置自动续期
```bash
# Certbot 会自动配置续期任务
# 查看续期任务
sudo systemctl status certbot.timer

# 手动续期
sudo certbot renew
```

## 10. 验证部署

### 10.1 检查服务状态
```bash
# 检查后端服务
sudo systemctl status japan-backend

# 检查 Nginx 服务
sudo systemctl status nginx

# 检查 MySQL 服务
sudo systemctl status mysql

# 检查所有服务
sudo systemctl status japan-backend nginx mysql
```

### 10.2 查看日志
```bash
# 后端日志
sudo journalctl -u japan-backend -n 100

# Nginx 访问日志
sudo tail -f /var/log/nginx/japan-customs-access.log

# Nginx 错误日志
sudo tail -f /var/log/nginx/japan-customs-error.log

# MySQL 错误日志
sudo tail -f /var/log/mysql/error.log
```

### 10.3 测试访问
```bash
# 测试前端
curl http://localhost/
curl http://your-domain.com/

# 测试后端 API
curl http://localhost:8000/api/v1/health
curl http://your-domain.com/api/v1/health

# 测试 API 文档
curl http://localhost:8000/docs
curl http://your-domain.com/docs

# 测试数据库连接
mysql -u app -papp123456 -e "SELECT VERSION();"
```

### 10.4 浏览器测试
- 前端：http://your-domain.com
- 后端 API：http://your-domain.com/api/v1/docs
- 登录测试
- 用户创建测试
- 任务创建测试
- 文件上传测试

## 11. 常用维护命令

### 11.1 服务管理
```bash
# 重启后端服务
sudo systemctl restart japan-backend

# 重启 Nginx
sudo systemctl restart nginx

# 重启 MySQL
sudo systemctl restart mysql

# 重启所有服务
sudo systemctl restart japan-backend nginx mysql

# 停止服务
sudo systemctl stop japan-backend
sudo systemctl stop nginx
sudo systemctl stop mysql

# 启动服务
sudo systemctl start japan-backend
sudo systemctl start nginx
sudo systemctl start mysql
```

### 11.2 查看服务状态
```bash
# 查看后端状态
sudo systemctl status japan-backend

# 查看 Nginx 状态
sudo systemctl status nginx

# 查看 MySQL 状态
sudo systemctl status mysql

# 查看所有服务状态
sudo systemctl status --no-pager
```

### 11.3 查看日志
```bash
# 实时查看后端日志
sudo journalctl -u japan-backend -f

# 查看最近100行后端日志
sudo journalctl -u japan-backend -n 100

# 查看今天的后端日志
sudo journalctl -u japan-backend --since today

# 查看 Nginx 访问日志
sudo tail -f /var/log/nginx/japan-customs-access.log

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/japan-customs-error.log
```

### 11.4 更新代码
```bash
# 进入项目目录
cd /opt/japan-customs-system/my_project_app

# 拉取最新代码
git pull origin master

# 更新后端依赖
cd backend
source .venv/bin/activate
pip install -r requirements.txt

# 重启后端服务
sudo systemctl restart japan-backend

# 更新前端
cd ../frontend
npm install
npm run build

# 部署前端
sudo cp -r dist/* /var/www/html/
sudo systemctl reload nginx
```

## 12. 备份和恢复

### 12.1 数据库备份
```bash
# 创建备份脚本
nano /opt/japan-customs-system/scripts/backup_database.sh
```

**备份脚本内容：**
```bash
#!/bin/bash

# 配置
BACKUP_DIR="/opt/japan-customs-system/backups"
DATE=$(date +%Y%m%d_%H%M%S)
DB_NAME="demo"
DB_USER="app"
DB_PASS="app123456"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
mysqldump -u $DB_USER -p$DB_PASS $DB_NAME | gzip > $BACKUP_DIR/backup_$DATE.sql.gz

# 删除30天前的备份
find $BACKUP_DIR -name "backup_*.sql.gz" -mtime +30 -delete

echo "Database backup completed: backup_$DATE.sql.gz"
```

```bash
# 设置执行权限
chmod +x /opt/japan-customs-system/scripts/backup_database.sh

# 手动执行备份
/opt/japan-customs-system/scripts/backup_database.sh
```

### 12.2 数据库恢复
```bash
# 从备份恢复
gunzip < /opt/japan-customs-system/backups/backup_20260224_120000.sql.gz | mysql -u app -papp123456 demo
```

### 12.3 文件备份
```bash
# 创建文件备份脚本
nano /opt/japan-customs-system/scripts/backup_files.sh
```

**文件备份脚本内容：**
```bash
#!/bin/bash

# 配置
BACKUP_DIR="/opt/japan-customs-system/backups"
DATE=$(date +%Y%m%d_%H%M%S)
SOURCE_DIR="/opt/japan-customs-system/storage"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份文件
tar czf $BACKUP_DIR/files_$DATE.tar.gz $SOURCE_DIR

# 删除30天前的备份
find $BACKUP_DIR -name "files_*.tar.gz" -mtime +30 -delete

echo "Files backup completed: files_$DATE.tar.gz"
```

### 12.4 完整备份
```bash
# 创建完整备份脚本
nano /opt/japan-customs-system/scripts/backup_all.sh
```

**完整备份脚本内容：**
```bash
#!/bin/bash

# 配置
BACKUP_DIR="/opt/japan-customs-system/backups"
DATE=$(date +%Y%m%d_%H%M%S)

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
/opt/japan-customs-system/scripts/backup_database.sh

# 备份文件
/opt/japan-customs-system/scripts/backup_files.sh

# 备份配置文件
tar czf $BACKUP_DIR/config_$DATE.tar.gz /opt/japan-customs-system/my_project_app/backend/.env

echo "Full backup completed: $DATE"
```

## 13. 监控和日志

### 13.1 系统监控
```bash
# 查看系统资源
htop

# 查看磁盘使用
df -h

# 查看内存使用
free -h

# 查看进程
ps aux

# 查看网络连接
netstat -tulpn
```

### 13.2 日志轮转配置
```bash
# 创建日志轮转配置
sudo nano /etc/logrotate.d/japan-customs-system
```

**配置内容：**
```
/opt/japan-customs-system/my_project_app/backend/logs/*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    create 0644 www-data www-data
    sharedscripts
    postrotate
        systemctl reload japan-backend > /dev/null 2>&1 || true
    endscript
}

/var/log/nginx/japan-customs-*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    create 0640 www-data adm
    sharedscripts
    postrotate
        systemctl reload nginx > /dev/null 2>&1 || true
    endscript
}
```

### 13.3 监控脚本
```bash
# 创建监控脚本
nano /opt/japan-customs-system/scripts/monitor.sh
```

**监控脚本内容：**
```bash
#!/bin/bash

# 检查服务状态
check_service() {
    if ! systemctl is-active --quiet $1; then
        echo "Service $1 is not running. Restarting..."
        systemctl restart $1
    fi
}

# 检查磁盘空间
check_disk() {
    DISK_USAGE=$(df / | awk 'NR==2 {print $5}' | sed 's/%//')
    if [ $DISK_USAGE -gt 80 ]; then
        echo "Warning: Disk usage is ${DISK_USAGE}%"
    fi
}

# 检查服务
check_service japan-backend
check_service nginx
check_service mysql

# 检查磁盘
check_disk
```

## 14. 故障排查

### 14.1 服务无法启动
```bash
# 查看服务状态
sudo systemctl status japan-backend

# 查看详细日志
sudo journalctl -u japan-backend -n 500

# 检查端口占用
sudo netstat -tulpn | grep :8000

# 检查配置文件
sudo nginx -t

# 检查权限
ls -la /opt/japan-customs-system/
```

### 14.2 数据库连接问题
```bash
# 检查 MySQL 状态
sudo systemctl status mysql

# 测试数据库连接
mysql -u app -papp123456 -e "SELECT 1;"

# 检查数据库用户权限
mysql -u root -p -e "SHOW GRANTS FOR 'app'@'localhost';"

# 查看 MySQL 错误日志
sudo tail -f /var/log/mysql/error.log

# 检查数据库连接数
mysql -u app -papp123456 -e "SHOW PROCESSLIST;"
```

### 14.3 Nginx 问题
```bash
# 测试 Nginx 配置
sudo nginx -t

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/error.log

# 检查 Nginx 权限
ls -la /var/www/html/

# 重启 Nginx
sudo systemctl restart nginx
```

### 14.4 权限问题
```bash
# 检查文件权限
ls -la /opt/japan-customs-system/
ls -la /var/www/html/

# 修复权限
sudo chown -R www-data:www-data /var/www/html
sudo chmod -R 755 /var/www/html

# 检查服务用户权限
sudo -u www-data ls -la /var/www/html/
```

### 14.5 网络问题
```bash
# 检查防火墙状态
sudo ufw status

# 检查端口监听
sudo netstat -tulpn

# 测试端口连接
telnet localhost 8000
telnet localhost 80

# 检查 DNS 解析
nslookup your-domain.com
```

## 15. 性能优化

### 15.1 MySQL 优化
```bash
# 编辑 MySQL 配置
sudo nano /etc/mysql/mysql.conf.d/mysqld.cnf
```

**MySQL 优化配置：**
```ini
[mysqld]
# 连接设置
max_connections = 200
max_connect_errors = 100000

# 缓冲区设置
innodb_buffer_pool_size = 1G
innodb_log_file_size = 256M
innodb_flush_log_at_trx_commit = 2

# 查询缓存
query_cache_size = 64M
query_cache_type = 1

# 慢查询日志
slow_query_log = 1
slow_query_log_file = /var/log/mysql/slow-query.log
long_query_time = 2

# 字符集
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci
```

### 15.2 Nginx 优化
```bash
# 编辑 Nginx 配置
sudo nano /etc/nginx/nginx.conf
```

**Nginx 优化配置：**
```nginx
user www-data;
worker_processes auto;
worker_rlimit_nofile 65535;

events {
    worker_connections 2048;
    use epoll;
    multi_accept on;
}

http {
    # 基础设置
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    keepalive_requests 100;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss;

    # 缓存设置
    proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=my_cache:10m max_size=1g inactive=60m;
    proxy_cache_key "$scheme$request_method$host$request_uri";

    # 包含站点配置
    include /etc/nginx/sites-enabled/*;
}
```

### 15.3 系统优化
```bash
# 编辑系统限制
sudo nano /etc/security/limits.conf
```

**系统限制配置：**
```
* soft nofile 65535
* hard nofile 65535
* soft nproc 65535
* hard nproc 65535
```

```bash
# 编辑内核参数
sudo nano /etc/sysctl.conf
```

**内核参数配置：**
```
# 网络优化
net.core.somaxconn = 65535
net.core.netdev_max_backlog = 5000
net.ipv4.tcp_max_syn_backlog = 8192
net.ipv4.tcp_tw_reuse = 1
net.ipv4.tcp_fin_timeout = 30

# 文件系统优化
fs.file-max = 65535
vm.swappiness = 10
```

```bash
# 应用内核参数
sudo sysctl -p
```

## 16. 安全建议

### 16.1 系统安全
```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装安全工具
sudo apt install fail2ban ufw -y

# 配置 fail2ban
sudo systemctl enable fail2ban
sudo systemctl start fail2ban
```

### 16.2 应用安全
```bash
# 修改默认密码
sudo mysql -u root -p
ALTER USER 'root'@'localhost' IDENTIFIED BY 'new-strong-password';
ALTER USER 'app'@'localhost' IDENTIFIED BY 'new-strong-password';
FLUSH PRIVILEGES;

# 限制 SSH 访问
sudo nano /etc/ssh/sshd_config
# 修改以下配置：
# PermitRootLogin no
# PasswordAuthentication no

# 重启 SSH
sudo systemctl restart sshd
```

### 16.3 文件权限
```bash
# 设置正确的文件权限
sudo chmod 600 /opt/japan-customs-system/my_project_app/backend/.env
sudo chmod 755 /opt/japan-customs-system/my_project_app/backend
sudo chmod 755 /opt/japan-customs-system/storage
```

### 16.4 日志安全
```bash
# 定期清理日志
sudo logrotate -f /etc/logrotate.conf

# 监控异常登录
sudo tail -f /var/log/auth.log
```

## 17. 生产环境检查清单

### 17.1 部署前检查
- [ ] 修改所有默认密码
- [ ] 配置防火墙规则
- [ ] 设置 SSL 证书
- [ ] 配置域名解析
- [ ] 测试数据库连接
- [ ] 测试 API 端点
- [ ] 配置日志轮转
- [ ] 设置监控告警
- [ ] 配置备份策略
- [ ] 测试恢复流程

### 17.2 部署后检查
- [ ] 验证所有服务正常运行
- [ ] 测试用户登录功能
- [ ] 测试文件上传功能
- [ ] 测试任务创建功能
- [ ] 检查日志无错误
- [ ] 验证数据库备份
- [ ] 测试 SSL 证书
- [ ] 检查资源使用情况
- [ ] 验证防火墙规则
- [ ] 测试备份恢复

## 18. 应急处理

### 18.1 服务崩溃
```bash
# 快速重启所有服务
sudo systemctl restart japan-backend nginx mysql

# 查看详细日志
sudo journalctl -u japan-backend -n 500
sudo tail -f /var/log/nginx/error.log
```

### 18.2 数据库损坏
```bash
# 从备份恢复
/opt/japan-customs-system/scripts/restore_database.sh

# 或者手动恢复
gunzip < /opt/japan-customs-system/backups/backup_latest.sql.gz | mysql -u app -papp123456 demo
```

### 18.3 磁盘空间不足
```bash
# 清理日志文件
sudo find /var/log -name "*.log" -mtime +7 -delete

# 清理 Docker 资素（如果使用 Docker）
docker system prune -a

# 清理旧的备份文件
find /opt/japan-customs-system/backups -name "*.sql.gz" -mtime +30 -delete
```

### 18.4 网络攻击
```bash
# 查看异常连接
sudo netstat -tulpn | grep ESTABLISHED

# 封禁攻击 IP
sudo ufw deny from 192.168.1.100

# 查看 fail2ban 状态
sudo fail2ban-client status
```

## 19. 联系和支持

- 技术支持：[技术支持邮箱]
- 文档地址：[文档地址]
- 问题反馈：[GitHub Issues]
- 紧急联系：[紧急联系电话]

## 附录

### A. 环境变量说明
| 变量名 | 说明 | 默认值 | 必填 |
|--------|------|--------|------|
| DATABASE_URL | 数据库连接URL | - | 是 |
| SECRET_KEY | JWT密钥 | - | 是 |
| ACCESS_TOKEN_EXPIRE_MINUTES | Token过期时间（分钟） | 1440 | 否 |
| DEEPSEEK_API_KEY | DeepSeek API密钥 | - | 是 |
| DEEPSEEK_API_URL | DeepSeek API地址 | https://api.deepseek.com/v1 | 否 |
| STORAGE_PATH | 存储路径 | /opt/japan-customs-system/storage | 否 |
| TASKS_STORAGE_PATH | 任务存储路径 | /opt/japan-customs-system/storage/tasks | 否 |
| LOG_LEVEL | 日志级别 | INFO | 否 |
| LOG_DIR | 日志目录 | /opt/japan-customs-system/my_project_app/backend/logs | 否 |

### B. 端口说明
| 端口 | 服务 | 说明 |
|------|------|------|
| 22 | SSH | 远程管理 |
| 80 | HTTP | Web访问 |
| 443 | HTTPS | 安全Web访问 |
| 8000 | 后端API | API服务 |
| 3306 | MySQL | 数据库连接 |

### C. 目录结构
```
/opt/japan-customs-system/
├── my_project_app/          # 项目代码
│   ├── backend/             # 后端代码
│   │   ├── .venv/          # Python虚拟环境
│   │   ├── logs/           # 日志文件
│   │   └── .env            # 环境变量
│   └── frontend/           # 前端代码
│       └── dist/           # 构建文件
├── storage/                # 存储文件
│   └── tasks/              # 任务文件
├── backups/                # 备份文件
└── scripts/                 # 脚本文件
    ├── backup_database.sh
    ├── backup_files.sh
    ├── backup_all.sh
    ├── cleanup_logs.sh
    └── monitor.sh
```

### D. 常见问题

**Q1: 如何修改服务运行用户？**
A: 编辑 `/etc/systemd/system/japan-backend.service`，修改 `User` 和 `Group` 字段。

**Q2: 如何增加上传文件大小限制？**
A: 在 Nginx 配置中添加 `client_max_body_size 100M;`，并重启 Nginx。

**Q3: 如何查看实时日志？**
A: 使用 `sudo journalctl -u japan-backend -f` 查看后端日志。

**Q4: 如何配置多个域名？**
A: 在 Nginx 配置中添加多个 `server` 块，每个域名一个。

**Q5: 如何优化数据库性能？**
A: 调整 MySQL 配置文件中的缓冲区大小、连接数等参数。

**Q6: 如何设置自动备份？**
A: 使用 crontab 定时执行备份脚本。

**Q7: 如何监控服务健康状态？**
A: 使用 systemd 的 `systemctl status` 或编写监控脚本。

**Q8: 如何升级 Python 版本？**
A: 使用 deadsnakes PPA 安装新版本，并重新创建虚拟环境。

**Q9: 如何配置负载均衡？**
A: 使用 Nginx 的 upstream 配置或专业的负载均衡器。

**Q10: 如何处理高并发？**
A: 优化数据库、使用缓存、增加服务器资源、使用负载均衡。

---

**文档版本：** 1.0.0
**最后更新：** 2026-02-24
**维护者：** 开发团队
**适用系统：** Ubuntu 20.04 LTS 及以上版本
