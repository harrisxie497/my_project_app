# 修改AI规则执行器的chat调用方式

## 目标
修改`ai_rule_executor.py`中的所有chat方法调用，都使用两个参数的方式（系统提示词和用户提示词），并且用户提示词的格式是JSON数组。

## 具体任务

### 1. 删除未使用的函数
- 删除第284行：`_handle_goods_en`方法
- 删除第716行：`_handle_material_en`方法
- 删除第798行：`_handle_translate_upper`方法

### 2. 修改批量处理方法的输入数据构建
- 修改`_handle_goods_en_batch`方法，使其输入数据符合JSON数组格式
- 修改`_handle_material_en_batch`方法，使其输入数据符合JSON数组格式
- 修改`_handle_text_ja_clean_batch`方法，使其输入数据符合JSON数组格式
- 修改`_handle_translate_upper_batch`方法，使其输入数据符合JSON数组格式
- 修改`_handle_translate_from_targetcol_en_upper_batch`方法，使其输入数据符合JSON数组格式
- 修改`_handle_text_ja_address_clean_batch`方法，使其输入数据符合JSON数组格式

### 3. 修改批量处理方法的chat调用
- 修改`_handle_goods_en_batch`方法，使用两个参数的方式（系统提示词和用户提示词）
- 修改`_handle_material_en_batch`方法，使用两个参数的方式（系统提示词和用户提示词）
- 修改`_handle_text_ja_clean_batch`方法，使用两个参数的方式（系统提示词和用户提示词）
- 修改`_handle_translate_upper_batch`方法，使用两个参数的方式（系统提示词和用户提示词）
- 修改`_handle_translate_from_targetcol_en_upper_batch`方法，使用两个参数的方式（系统提示词和用户提示词）
- 修改`_handle_text_ja_address_clean_batch`方法，使用两个参数的方式（系统提示词和用户提示词）

### 4. 用户提示词的格式
用户提示词的格式需要是JSON数组，包含index和context字段。例如：
```json
[
  {"index": "1", "context": "日文品名"},
  {"index": "2", "context": "日文品名"},
  ...
]
```

## 预期效果
- 所有chat方法调用都使用两个参数的方式
- 用户提示词的格式是JSON数组
- 输入数据构建符合JSON数组格式
- 删除未使用的函数，减少代码冗余