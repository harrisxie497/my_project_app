# 日本清关Excel自动生成系统 - Docker容器化部署文档

## 目录
1. [拉取最新代码](#1-拉取最新代码)
2. [Docker容器化部署步骤](#2-docker容器化部署步骤)
3. [创建Dockerfile](#3-创建dockerfile)
4. [创建docker-compose.yml](#4-创建docker-composeyml)
5. [创建.env文件](#5-创建env文件)
6. [构建和启动容器](#6-构建和启动容器)
7. [常用Docker命令](#7-常用docker命令)
8. [数据库初始化](#8-数据库初始化)
9. [备份和恢复](#9-备份和恢复)
10. [监控和日志](#10-监控和日志)
11. [更新部署流程](#11-更新部署流程)
12. [故障排查](#12-故障排查)
13. [性能优化建议](#13-性能优化建议)
14. [安全建议](#14-安全建议)

## 1. 拉取最新代码

```bash
# 进入项目目录
cd /opt/japan-customs-system/my_project_app

# 拉取最新代码
git pull origin master
```

## 2. Docker容器化部署步骤

### 2.1 环境要求
- Docker 20.10+
- Docker Compose 2.0+
- 服务器内存至少 2GB
- 磁盘空间至少 10GB

### 2.2 安装Docker
```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 安装 Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 验证安装
docker --version
docker-compose --version
```

## 3. 创建Dockerfile

### 3.1 后端Dockerfile
创建文件：`backend/Dockerfile`

```dockerfile
FROM python:3.10-slim

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    default-libmysqlclient-dev \
    pkg-config \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件
COPY requirements.txt .

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY . .

# 创建存储目录
RUN mkdir -p /app/storage/tasks

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 3.2 前端Dockerfile
创建文件：`frontend/Dockerfile`

```dockerfile
# 构建阶段
FROM node:18-alpine as build

WORKDIR /app

# 复制依赖文件
COPY package*.json ./

# 安装依赖
RUN npm install

# 复制源代码
COPY . .

# 构建应用
RUN npm run build

# 生产阶段
FROM nginx:alpine

# 复制构建文件
COPY --from=build /app/dist /usr/share/nginx/html

# 复制nginx配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 暴露端口
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

### 3.3 前端nginx配置
创建文件：`frontend/nginx.conf`

```nginx
server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # 前端路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端API代理
    location /api {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 文件下载代理
    location /storage {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
    }
}
```

## 4. 创建docker-compose.yml

创建文件：`docker-compose.yml`

```yaml
version: '3.8'

services:
  # MySQL数据库
  mysql:
    image: mysql:8.0
    container_name: japan-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root123456
      MYSQL_DATABASE: demo
      MYSQL_USER: app
      MYSQL_PASSWORD: app123456
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - japan-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 后端服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: japan-backend
    restart: always
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: mysql+pymysql://app:app123456@mysql:3306/demo?charset=utf8mb4
      SECRET_KEY: your-secret-key-change-this-in-production
      ACCESS_TOKEN_EXPIRE_MINUTES: 1440
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
      DEEPSEEK_API_URL: https://api.deepseek.com/v1
      STORAGE_PATH: /app/storage
      TASKS_STORAGE_PATH: /app/storage/tasks
    volumes:
      - storage_data:/app/storage
      - ./backend/logs:/app/logs
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - japan-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # 前端服务
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: japan-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - japan-network

volumes:
  mysql_data:
    driver: local
  storage_data:
    driver: local

networks:
  japan-network:
    driver: bridge
```

## 5. 创建.env文件

创建文件：`.env`

```bash
# DeepSeek API Key
DEEPSEEK_API_KEY=your-deepseek-api-key-here
```

## 6. 构建和启动容器

```bash
# 拉取最新代码
git pull origin master

# 构建并启动所有服务
docker-compose up -d --build

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

## 7. 常用Docker命令

### 7.1 容器管理
```bash
# 查看运行中的容器
docker ps

# 查看所有容器（包括停止的）
docker ps -a

# 查看容器日志
docker logs japan-backend
docker logs japan-frontend
docker logs japan-mysql

# 进入容器
docker exec -it japan-backend /bin/bash
docker exec -it japan-mysql mysql -u app -p

# 重启服务
docker-compose restart backend
docker-compose restart frontend
docker-compose restart mysql

# 停止所有服务
docker-compose down

# 停止并删除数据卷
docker-compose down -v

# 查看资源使用情况
docker stats
```

### 7.2 镜像管理
```bash
# 查看本地镜像
docker images

# 删除未使用的镜像
docker image prune -a

# 删除特定镜像
docker rmi image-name:tag
```

### 7.3 网络管理
```bash
# 查看网络
docker network ls

# 查看网络详情
docker network inspect japan-network

# 删除网络
docker network rm japan-network
```

### 7.4 卷管理
```bash
# 查看卷
docker volume ls

# 查看卷详情
docker volume inspect japan_storage_data

# 删除未使用的卷
docker volume prune
```

## 8. 数据库初始化

```bash
# 等待MySQL启动
sleep 10

# 进入后端容器初始化数据库
docker exec -it japan-backend python -c "from app.core.database import init_db; init_db()"

# 或者直接执行SQL文件
docker exec -i japan-mysql mysql -u app -papp123456 demo < init.sql
```

## 9. 备份和恢复

### 9.1 数据库备份
```bash
# 备份数据库
docker exec japan-mysql mysqldump -u app -papp123456 demo > backup_$(date +%Y%m%d_%H%M%S).sql

# 备份到压缩文件
docker exec japan-mysql mysqldump -u app -papp123456 demo | gzip > backup_$(date +%Y%m%d_%H%M%S).sql.gz
```

### 9.2 数据库恢复
```bash
# 恢复数据库
cat backup.sql | docker exec -i japan-mysql mysql -u app -papp123456 demo

# 从压缩文件恢复
gunzip < backup.sql.gz | docker exec -i japan-mysql mysql -u app -papp123456 demo
```

### 9.3 存储文件备份
```bash
# 备份存储文件
docker run --rm -v japan_storage_data:/data -v $(pwd):/backup alpine tar czf /backup/storage-backup_$(date +%Y%m%d_%H%M%S).tar.gz /data

# 恢复存储文件
docker run --rm -v japan_storage_data:/data -v $(pwd):/backup alpine tar xzf /backup/storage-backup.tar.gz -C /
```

## 10. 监控和日志

### 10.1 实时查看日志
```bash
# 查看所有服务日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f mysql

# 查看最近100行日志
docker logs --tail 100 japan-backend
```

### 10.2 容器监控
```bash
# 查看容器资源使用情况
docker stats

# 查看容器详细信息
docker inspect japan-backend

# 查看容器进程
docker exec japan-backend ps aux
```

### 10.3 健康检查
```bash
# 检查MySQL健康状态
docker exec japan-mysql mysqladmin ping -h localhost

# 检查后端健康状态
curl http://localhost:8000/api/v1/health

# 检查前端状态
curl http://localhost:80
```

## 11. 更新部署流程

```bash
# 1. 拉取最新代码
git pull origin master

# 2. 停止并删除旧容器
docker-compose down

# 3. 重新构建并启动
docker-compose up -d --build

# 4. 查看启动状态
docker-compose ps

# 5. 查看日志确认启动成功
docker-compose logs -f
```

### 11.1 只更新某个服务
```bash
# 只重新构建后端
docker-compose up -d --build backend

# 只重新构建前端
docker-compose up -d --build frontend
```

### 11.2 滚动更新
```bash
# 使用Watchtower自动更新容器
docker run -d \
  --name watchtower \
  -v /var/run/docker.sock:/var/run/docker.sock \
  containrrr/watchtower
```

## 12. 故障排查

### 12.1 容器无法启动
```bash
# 检查容器状态
docker ps -a

# 查看容器日志
docker logs japan-backend

# 检查容器配置
docker inspect japan-backend

# 检查端口占用
netstat -tulpn | grep :8000
netstat -tulpn | grep :3306
```

### 12.2 网络连接问题
```bash
# 检查网络连接
docker network ls
docker network inspect japan-network

# 测试容器间连接
docker exec japan-backend ping mysql

# 检查DNS解析
docker exec japan-backend nslookup mysql
```

### 12.3 数据库连接问题
```bash
# 进入MySQL容器
docker exec -it japan-mysql mysql -u app -p

# 检查数据库权限
SHOW GRANTS FOR 'app'@'%';

# 检查数据库连接数
SHOW PROCESSLIST;

# 检查数据库大小
SELECT table_schema AS 'Database', 
       ROUND(SUM(data_length + index_length) / 1024 / 1024, 2) AS 'Size (MB)' 
FROM information_schema.tables 
GROUP BY table_schema;
```

### 12.4 清理无用资源
```bash
# 清理停止的容器
docker container prune

# 清理未使用的镜像
docker image prune -a

# 清理未使用的卷
docker volume prune

# 清理未使用的网络
docker network prune

# 清理所有无用资源
docker system prune -a
```

## 13. 性能优化建议

### 13.1 资源限制
在`docker-compose.yml`中添加资源限制：

```yaml
services:
  backend:
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G

  mysql:
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G
```

### 13.2 MySQL优化
```yaml
services:
  mysql:
    command: [
      '--default-authentication-plugin=mysql_native_password',
      '--character-set-server=utf8mb4',
      '--collation-server=utf8mb4_unicode_ci',
      '--max-connections=200',
      '--innodb-buffer-pool-size=1G'
    ]
```

### 13.3 后端优化
```yaml
services:
  backend:
    command: [
      'uvicorn',
      'app.main:app',
      '--host', '0.0.0.0',
      '--port', '8000',
      '--workers', '4',
      '--worker-class', 'uvicorn.workers.UvicornWorker'
    ]
```

### 13.4 Nginx缓存优化
在`frontend/nginx.conf`中添加缓存配置：

```nginx
proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=my_cache:10m max_size=1g inactive=60m;

location /api {
    proxy_cache my_cache;
    proxy_cache_valid 200 10m;
    proxy_cache_key "$scheme$request_method$host$request_uri";
    
    proxy_pass http://backend:8000;
    proxy_set_header Host $host;
}
```

## 14. 安全建议

### 14.1 使用Secrets管理敏感信息
```bash
# 创建JWT secret
echo "your-secret-key" | docker secret create jwt_secret -

# 创建MySQL密码
echo "your-mysql-password" | docker secret create mysql_password -

# 在docker-compose.yml中使用
services:
  backend:
    secrets:
      - jwt_secret
    environment:
      SECRET_KEY_FILE: /run/secrets/jwt_secret
```

### 14.2 限制容器权限
```yaml
services:
  backend:
    user: "1000:1000"
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid,size=100m
```

### 14.3 网络隔离
```yaml
networks:
  japan-network:
    driver: bridge
    internal: false
    ipam:
      config:
        - subnet: 172.20.0.0/24
```

### 14.4 只开放必要端口
```yaml
services:
  backend:
    ports:
      - "127.0.0.1:8000:8000"  # 只允许本地访问
```

### 14.5 SSL/TLS配置
```yaml
services:
  frontend:
    ports:
      - "443:443"
    volumes:
      - ./ssl/cert.pem:/etc/nginx/cert.pem:ro
      - ./ssl/key.pem:/etc/nginx/key.pem:ro
```

## 15. 生产环境检查清单

### 15.1 部署前检查
- [ ] 修改所有默认密码
- [ ] 配置防火墙规则
- [ ] 设置SSL证书
- [ ] 配置域名解析
- [ ] 测试数据库连接
- [ ] 测试API端点
- [ ] 配置日志轮转
- [ ] 设置监控告警

### 15.2 部署后检查
- [ ] 验证所有服务正常运行
- [ ] 测试用户登录功能
- [ ] 测试文件上传功能
- [ ] 测试任务创建功能
- [ ] 检查日志无错误
- [ ] 验证数据库备份
- [ ] 测试SSL证书
- [ ] 检查资源使用情况

## 16. 应急处理

### 16.1 服务崩溃
```bash
# 快速重启所有服务
docker-compose restart

# 如果无法启动，查看详细日志
docker-compose logs --tail=500
```

### 16.2 数据库损坏
```bash
# 从备份恢复
docker-compose down
docker volume rm japan_mysql_data
docker-compose up -d
cat backup.sql | docker exec -i japan-mysql mysql -u app -papp123456 demo
```

### 16.3 磁盘空间不足
```bash
# 清理Docker资源
docker system prune -a

# 清理日志文件
docker exec japan-backend find /app/logs -name "*.log" -mtime +7 -delete

# 清理旧的备份文件
find . -name "backup_*.sql" -mtime +30 -delete
```

## 17. 联系和支持

- 技术支持：[技术支持邮箱]
- 文档地址：[文档地址]
- 问题反馈：[GitHub Issues]

## 附录

### A. 环境变量说明
| 变量名 | 说明 | 默认值 | 必填 |
|--------|------|--------|------|
| DATABASE_URL | 数据库连接URL | - | 是 |
| SECRET_KEY | JWT密钥 | - | 是 |
| ACCESS_TOKEN_EXPIRE_MINUTES | Token过期时间（分钟） | 1440 | 否 |
| DEEPSEEK_API_KEY | DeepSeek API密钥 | - | 是 |
| DEEPSEEK_API_URL | DeepSeek API地址 | https://api.deepseek.com/v1 | 否 |
| STORAGE_PATH | 存储路径 | /app/storage | 否 |
| TASKS_STORAGE_PATH | 任务存储路径 | /app/storage/tasks | 否 |

### B. 端口说明
| 端口 | 服务 | 说明 |
|------|------|------|
| 80 | 前端 | HTTP访问 |
| 443 | 前端 | HTTPS访问 |
| 8000 | 后端 | API服务 |
| 3306 | MySQL | 数据库连接 |

### C. 常见问题

**Q1: 容器启动失败怎么办？**
A: 检查端口占用、查看容器日志、验证配置文件

**Q2: 如何查看容器内部文件？**
A: 使用 `docker exec -it container_name /bin/bash` 进入容器

**Q3: 如何持久化数据？**
A: 使用Docker volumes挂载数据目录

**Q4: 如何升级Docker版本？**
A: 参考Docker官方文档进行升级

**Q5: 容器之间如何通信？**
A: 使用Docker网络，通过容器名称访问

---

**文档版本：** 1.0.0
**最后更新：** 2026-02-24
**维护者：** 开发团队
